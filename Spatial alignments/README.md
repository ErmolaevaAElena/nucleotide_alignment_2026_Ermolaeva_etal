# Spatial alignments

Spatial alignments of the resulting S-protein models (with and without the insertion), constructed in ChimeraX using the MatchMaker tool with selected structures from the open-access PDB database
